// The heroic elements have been merged into the main background (ThreeBackground.jsx).
// We return null here to prevent rendering layered wireframes which clash with the requested clean black + crypto coin aesthetic.
export default function HeroScene() {
  return null;
}
